# Runtime dependencies for the FastAPI backend only (used by the Dockerfile).
#
# NOTE: `torch` is intentionally NOT listed here - the Dockerfile installs it
# separately from the CPU-only index (https://download.pytorch.org/whl/cpu) to
# avoid pulling ~2GB of unused CUDA wheels.  transformers requires torch, which
# is why the CPU install must run first.

langchain-core>=0.3
langchain-chroma>=0.2
langchain-google-genai>=2.0
langchain-huggingface>=0.1
transformers>=4.40

fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
Pillow>=10.0
